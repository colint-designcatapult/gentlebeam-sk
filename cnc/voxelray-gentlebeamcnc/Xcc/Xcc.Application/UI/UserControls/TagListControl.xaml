<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ucx="clr-namespace:Xcc.Application.UI.UserControls">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Xcc.Styles;component/Styles/Button/ButtonCrossStyle.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/Xcc.Styles;Component/Styles/ScrollViewer/ScrollViewerCrossStyle.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="TagListControlItem" TargetType="ListBoxItem">
        <Setter Property="Margin" Value="0,0,8,0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border 
                        Background="{StaticResource BlockBackgroundBrush}" 
                        CornerRadius="6">
                        
                        <Grid Margin="8,0,4,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="32"/>
                            </Grid.ColumnDefinitions>

                            <ContentPresenter VerticalAlignment="Center" Margin="4,0"/>

                            <Button 
                                IsTabStop="False"
                                Margin="2,0,-4,0"
                                Style="{StaticResource ButtonCrossStyle}"
                                Grid.Column="1" 
                                Command="ApplicationCommands.Delete" CommandParameter="{Binding}"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ucx:TagListControl">
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource TagListControlItem}"/>
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ucx:TagListControl">
                    <ScrollViewer Focusable="False" Style="{StaticResource ScrollViewerCrossStyle}">
                        <StackPanel Orientation="Horizontal" IsItemsHost="True" Margin="2,0"/>
                    </ScrollViewer>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="OpacityMask" Value="{StaticResource DisabledControlOpacityMaskBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>