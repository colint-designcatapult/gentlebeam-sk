<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Xcc.Styles;Component/Styles/BaseStyle.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/Xcc.Styles;Component/Styles/TextBlock/TextBlockLegendStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="ProgressBarXRayStyle" TargetType="{x:Type ProgressBar}">
        
        <Setter Property="IsIndeterminate" Value="False" />
        <Setter Property="Background" Value="Yellow"/>
        <Setter Property="Foreground" Value="{StaticResource FieldForegroundColor}"/>
        <Setter Property="Border.CornerRadius" Value="{StaticResource BlockCornerRadius}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar" >
                    <Grid x:Name="TemplateRoot" SnapsToDevicePixels="true">
                        <Border 
                            x:Name="MaskBorder" 
                            CornerRadius="{Binding Path=(Border.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}"
                            Background="{StaticResource ButtonBackgroundBrush}"/>

                        <Grid>
                            <Grid.OpacityMask>
                                <VisualBrush Visual="{Binding ElementName=MaskBorder}"/>
                            </Grid.OpacityMask>

                            <Border x:Name="PART_Track" 
                                Background="{StaticResource ButtonBackgroundBrush}"
                                CornerRadius="{Binding Path=(Border.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}"/>

                            <Border 
                                x:Name="PART_Indicator" 
                                HorizontalAlignment="Left"
                                Background="{TemplateBinding Background}">

  
                            </Border>

                            <TextBlock 
                                Style="{StaticResource TextBlockLegendStyle}" HorizontalAlignment="Center">
                                
                                <Run Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <Run Text="{Binding Value, RelativeSource={RelativeSource TemplatedParent}, StringFormat={}{0:F0}%}"/> 
                            </TextBlock>
                        </Grid>

                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>