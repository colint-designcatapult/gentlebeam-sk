<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Xcc.Styles;Component/Styles/BaseStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="GroupBoxDefaultStyle" TargetType="GroupBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GroupBox}">
                    <Grid SnapsToDevicePixels="true">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="6"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="6"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="6"/>
                        </Grid.RowDefinitions>

                        <Border 
                            Grid.Column="0"
                            Grid.ColumnSpan="4" 
                            Grid.Row="1"
                            Grid.RowSpan="3"
                            BorderBrush="Transparent"
                            BorderThickness="1"
                            Background="{TemplateBinding Background}" 
                            CornerRadius="4"/>

                        <ContentPresenter 
                            Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="2" 
                            Margin="{TemplateBinding Padding}" 
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>

                        <Border 
                            Grid.Column="0" Grid.ColumnSpan="4" Grid.Row="1" Grid.RowSpan="3"
                            BorderThickness="1" 
                            BorderBrush="{StaticResource GroupBoxBorderBrush}" 
                            CornerRadius="{StaticResource GroupBoxCornerRadius}">

                            <Border.OpacityMask>
                                <MultiBinding ConverterParameter="16">
                                    <MultiBinding.Converter>
                                        <BorderGapMaskConverter />
                                    </MultiBinding.Converter>
                                    <Binding ElementName="PART_Header" Path="ActualWidth"/>
                                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource Self}"/>
                                    <Binding Path="ActualHeight" RelativeSource="{RelativeSource Self}"/>
                                </MultiBinding>
                            </Border.OpacityMask>

                            <Border 
                                BorderBrush="{StaticResource GroupBoxInnerBorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="{StaticResource GroupBoxInnerCornerRadius}"/>
                        </Border>

                        <ContentControl 
                            x:Name="PART_Header" 
                            Grid.Column="1" Grid.Row="0" Grid.RowSpan="2"
                            Foreground="{StaticResource ForegroundAccentBrush}"
                            FontSize="{StaticResource LargeFontSize}"
                            FontWeight="SemiBold"
                            IsTabStop="False"
                            Margin="10,0,0,0">

                            <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </ContentControl>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--This style will be applied automatically to all buttons without explicit style-->
    <Style TargetType="{x:Type GroupBox}" BasedOn="{StaticResource GroupBoxDefaultStyle}"/>
</ResourceDictionary>