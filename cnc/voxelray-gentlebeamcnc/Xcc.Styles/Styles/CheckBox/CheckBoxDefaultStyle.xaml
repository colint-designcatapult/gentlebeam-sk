<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Xcc.Styles;Component/Styles/BaseStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="CheckBoxDefaultStyle" TargetType="CheckBox">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundDefaultBrush}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <BulletDecorator Background="Transparent" VerticalAlignment="Center">
                        <BulletDecorator.Bullet>
                            <Grid>
                                <Viewbox 
                                    x:Name="PART_CheckMark"
                                    Width="24" Height="24" 
                                    Visibility="Collapsed">

                                    <Path 
                                        x:Name="PART_CheckMarkGlyph" 
                                        UseLayoutRounding="True"
                                        SnapsToDevicePixels="False"
                                        Fill="{StaticResource ButtonStrokeBrush}">

                                        <Path.Data>
                                            <CombinedGeometry GeometryCombineMode="Exclude">
                                                <CombinedGeometry.Geometry1>
                                                    <RectangleGeometry Rect="0,0,24,24" RadiusX="6" RadiusY="6"/>
                                                </CombinedGeometry.Geometry1>
                                                <CombinedGeometry.Geometry2>
                                                    <PathGeometry Figures="m 5 11 L 9 15 L 19 5 L 21 7 L 9 19 L 3 13"/>
                                                </CombinedGeometry.Geometry2>
                                            </CombinedGeometry>
                                        </Path.Data>
                                    </Path>
                                </Viewbox>

                                <Border 
                                    x:Name="PART_Border"
                                    Width="24" Height="24"
                                    CornerRadius="6"
                                    BorderThickness="2"
                                    BorderBrush="{StaticResource ButtonStrokeBrush}"/>

                                <Viewbox 
                                    x:Name="PART_IndeterminateMark" 
                                    Width="24" Height="24"  
                                    Visibility="Collapsed">

                                    <Path 
                                        x:Name="PART_IndeterminateMarkGlyph" 
                                        UseLayoutRounding="True"
                                        SnapsToDevicePixels="False"
                                        Fill="{StaticResource ButtonStrokeBrush}">

                                        <Path.Data>
                                            <CombinedGeometry GeometryCombineMode="Exclude">
                                                <CombinedGeometry.Geometry1>
                                                    <RectangleGeometry Rect="0,0,24,24" RadiusX="6" RadiusY="6"/>
                                                </CombinedGeometry.Geometry1>
                                                <CombinedGeometry.Geometry2>
                                                    <PathGeometry Figures="m 5 11 L 19 11 L 19 14 L 5 14"/>
                                                </CombinedGeometry.Geometry2>
                                            </CombinedGeometry>
                                        </Path.Data>
                                    </Path>
                                </Viewbox>
                            </Grid>
                        </BulletDecorator.Bullet>

                        <ContentPresenter 
                            Margin="{StaticResource BlockContentSmallMarginLeft}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                            RecognizesAccessKey="True"/>
                    </BulletDecorator>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="PART_CheckMark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="PART_IndeterminateMark" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="PART_Border" Property="Background" Value="{StaticResource ControlIsMouseOverBackground}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter TargetName="PART_Border" Property="BorderBrush" Value="{StaticResource ForegroundFocusedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource ForegroundPaleBrush}" />
                            <Setter TargetName="PART_Border" Property="BorderBrush" Value="{StaticResource ButtonDisabledStrokeBrush}" />
                            <Setter TargetName="PART_CheckMarkGlyph" Property="Fill" Value="{StaticResource ButtonDisabledStrokeBrush}" />
                            <Setter TargetName="PART_IndeterminateMarkGlyph" Property="Fill" Value="{StaticResource ButtonDisabledStrokeBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--This style will be applied automatically to all CheckBoxes without explicit style-->
    <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource CheckBoxDefaultStyle}"/>
</ResourceDictionary>