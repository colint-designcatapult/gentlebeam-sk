<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Xcc.Styles;Component/Styles/BaseStyle.xaml" />
        <ResourceDictionary Source="pack://application:,,,/Xcc.Styles;Component/Resources/Icons/InterlockIcons.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="InterlocksIndicatorStyle" TargetType="ContentControl" >
        <Setter Property="Height" Value="22"/>
        <Setter Property="Width" Value="22"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>  

    <Style x:Key="CheckBoxThumbStyle" TargetType="CheckBox">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="IsThreeState" Value="True"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundDefaultBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="IsEnabled" Value="False"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="{StaticResource BlockContentSmallMarginLength}"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid>
                            <ContentControl 
                                x:Name="IndeterminateIcon"
                                Style="{StaticResource InterlocksIndicatorStyle}"
                                Content="{StaticResource ThumbUnknownSvgIcon}"
                                Visibility="Visible"/>
        
                            <ContentControl 
                                x:Name="ThumbDownIcon"
                                Style="{StaticResource InterlocksIndicatorStyle}" 
                                Content="{StaticResource ThumbDislikeSvgIcon}"
                                Visibility="Hidden"/>
        
                            <ContentControl 
                                x:Name="ThumbUpIcon"
                                Style="{StaticResource InterlocksIndicatorStyle}" 
                                Content="{StaticResource ThumbLikeSvgIcon}" 
                                Visibility="Hidden"/>
                        </Grid>

                        <ContentPresenter Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Left" RecognizesAccessKey="True"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ThumbUpIcon" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ThumbDownIcon" Property="Visibility" Value="Hidden"/>
                            <Setter TargetName="IndeterminateIcon" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter TargetName="ThumbUpIcon" Property="Visibility" Value="Hidden"/>
                            <Setter TargetName="ThumbDownIcon" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="IndeterminateIcon" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="ThumbUpIcon" Property="Visibility" Value="Hidden"/>
                            <Setter TargetName="ThumbDownIcon" Property="Visibility" Value="Hidden"/>
                            <Setter TargetName="IndeterminateIcon" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>