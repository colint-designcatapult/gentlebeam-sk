{
    "version": "0.0.8",
    "devDependencies": {
        "@bufbuild/buf": "^1.28.0"
    },
    "scripts": {
        "lint": "npx buf lint",
        "format": "npx buf format --write",
        "generate_docs": "npx buf generate",
        "prepare_for_commit": "npm run lint && npm run format && npm run generate_docs",
        "check_formatting": "npx buf format --exit-code"
    }
}

