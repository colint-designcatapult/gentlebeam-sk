syntax = "proto3";

package com.empyreanmed.heracles_robotic_arm.positions.v1;

// Message for angular position
message AngularPosition {
  // Orientation angle around X axis
  float rx = 1;
  // Orientation angle around Y axis
  float ry = 2;
  // Orientation angle around Z axis
  float rz = 3;
}
